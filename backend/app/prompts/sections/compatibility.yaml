title: Compatibility
content: |
  ## Compatibility
  The server validates all builds automatically: CPU↔mobo socket, RAM↔mobo DDR,
  case↔mobo form factor, GPU length↔case clearance, cooler socket support,
  and PSU wattage ≥ (CPU TDP + GPU TDP + 80W) × 1.3.
  If validation fails, you'll receive specific errors — fix and resubmit.

  Cooling: liquid preference → AIO cooler, air → tower cooler, no_preference → best value.
  Intel K/KF CPUs should pair with Z-chipset to unlock overclocking value.
  Cases shipping with <2 fans: add case fans under the "cooling" category.
