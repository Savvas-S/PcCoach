title: Compatibility
content: |
  ## Compatibility
  Server validates: CPU↔mobo socket, RAM↔mobo DDR, case↔mobo form factor,
  GPU length↔case clearance, cooler socket support,
  PSU wattage ≥ (CPU TDP + GPU TDP + 80W) × 1.3.
  Fix and resubmit on failure.

  Cooling: liquid → AIO, air → tower, no_preference → best value.
  Intel K/KF → Z-chipset. Cases with <2 fans: add fans under "cooling".

  Soft constraints (not server-checked):
  RAM speed within mobo range, air cooler height fits case, radiator size matches case mounts.
