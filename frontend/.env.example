# Server-side URL used by Next.js to proxy /api/v1/* to the backend.
# In Docker this is set via docker-compose environment (BACKEND_URL=http://backend:8000).
# For local dev without Docker, leave unset (defaults to http://localhost:8000).
# BACKEND_URL=http://localhost:8000
